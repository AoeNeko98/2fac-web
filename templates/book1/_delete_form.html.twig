<form method="post" action="{{ path('book1_delete', {'id': book1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book1.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
