<form method="post" action="{{ path('eleve_delete', {'idUser': eleve.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.idUser) }}">
    <button class="btn">Delete</button>
</form>
