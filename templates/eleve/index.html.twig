{% extends 'base-houssem.html.twig' %}

{% block title %}Eleve index{% endblock %}

{% block body %}
    <h1>Eleve index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdUser</th>
                <th>Score</th>
                <th>BacType</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eleve in eleves %}
            <tr>
                <td>{{ eleve.idUser }}</td>
                <td>{{ eleve.score }}</td>
                <td>{{ eleve.bacType }}</td>
                <td>
                    <a href="{{ path('eleve_show', {'idUser': eleve.idUser}) }}">show</a>
                    <a href="{{ path('eleve_edit', {'idUser': eleve.idUser}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('eleve_new') }}">Create new</a>
{% endblock %}
