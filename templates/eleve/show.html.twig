{% extends 'base-houssem.html.twig' %}

{% block title %}Eleve{% endblock %}

{% block body %}
    <h1>Eleve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdUser</th>
                <td>{{ eleve.idUser }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ eleve.score }}</td>
            </tr>
            <tr>
                <th>BacType</th>
                <td>{{ eleve.bacType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eleve_index') }}">back to list</a>

    <a href="{{ path('eleve_edit', {'idUser': eleve.idUser}) }}">edit</a>

    {{ include('eleve/_delete_form.html.twig') }}
{% endblock %}
