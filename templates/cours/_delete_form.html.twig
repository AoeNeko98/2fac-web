<form method="post" action="{{ path('cours_delete', {'idCours': cour.idCours,'id_Etab':cour.Id.Id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.idCours) }}">
    <button class="btn">Delete</button>
</form>
