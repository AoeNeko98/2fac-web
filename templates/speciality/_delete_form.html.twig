<form method="post" action="{{ path('speciality_delete', {'idSpec': speciality.idSpec}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ speciality.idSpec) }}">
    <button class="btn">Delete</button>
</form>
