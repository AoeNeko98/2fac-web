<form method="post" action="{{ path('commentaires_delete', {'idComnt': commentaire.idComnt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idComnt) }}">
    <button class="btn">Delete</button>
</form>
